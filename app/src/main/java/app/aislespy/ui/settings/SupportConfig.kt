package app.aislespy.ui.settings

/**
 * Donation / support constants used by [SupportScreen].
 *
 * Single obvious placeholder so the owner can drop in the real address later.
 * Presentation only — no network hosts are added for donations.
 */
object SupportConfig {
    // Owner's Bitcoin (BTC) donation address — native SegWit (bech32).
    const val BITCOIN_ADDRESS = "bc1qk0f9xlkvc44ue7gvvvhame5fy85j4nz0cyuwv0"
}
